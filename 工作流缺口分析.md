---
title: 工作流缺口分析
created: 2026-06-05
updated: 2026-06-05
status: 草稿
type: 技术
tags: [ai, dev-workflow, trellis, gitnexus, serena]
aliases: [工作流缺失项, 工作流改进点]
---

# 工作流缺口分析

当前 [[Trellis + GitNexus + Serena 工作流]] 的核心链条是完整的（规范 → 看懂 → 改准），但上下游和侧翼存在五个缺口。

## 1. 测试环节太弱

**现状**：Trellis check 只做 lint/typecheck/diff 审查，缺少真正的测试闭环。

**缺什么**：

- [[Code Change Workflow]] 里的 TDD（红 → 绿 → 重构）没融进来——先写失败测试证明确实有 bug，再最小修复，再回归验证
- 社区帖子里强调的「逐项核对 PRD/Spec 表格」没落实到流程步骤中
- 没有回归测试策略：修完的 bug 过段时间可能悄悄坏掉

**理想状态**：

```
实现 → 针对性测试 → 完整测试套件 → 对照 Spec 逐项核对 → 通过才进 check
```

## 2. 多模型分工没体现

**现状**：Trellis 本身不关心背后跑的什么模型，工作流里没有模型选择策略。

**缺什么**：

- [[Trellis 工序与工具链]] 里社区的核心洞察：**Opus 4.7 管 spec 生成（想清楚要做什么），GPT-5.5 管写代码（自纠错能力强）**
- brainstorm 和 spec 阶段用什么模型？实现阶段用什么模型？没有明确
- 双模型校验（先 Opus 跑一轮 PRD，再 GPT 补充一轮）没进流程

**理想状态**：

```
brainstorm → Opus 4.7 出 spec → GPT-5.5 做校验补充 → Opus 4.7 解耦成 PRD
实现阶段 → GPT-5.5 写代码（自纠错强）→ 人工核对
```

## 3. 辅助工具层缺失

**现状**：三件套只覆盖了「规范 + 看懂 + 改准」，但调研、推理、浏览器验证都没串进来。

**缺什么**：

| 场景 | 缺失工具 | 作用 |
| ---- | -------- | ---- |
| 搜索调研 | Grok-Search + Firecrawl | 查最新技术方案、API 文档、社区方案 |
| 复杂推理 | sequential-thinking | 多步推理兜底，任何时候都不过时 |
| 浏览器验证 | Playwright MCP | UI 改完没开浏览器看效果 = 没验收 |
| 前端组件 | shadcn MCP | React/Vue 组件库，省得 AI 瞎造 |
| 记忆管理 | memory MCP | 跨会话记住项目上下文 |

相关工具笔记：[[Sequential Thinking]] · [[Smithery]]

[[Trellis 工序与工具链]] 里已经记了完整工具链推荐，但没串进实际工作流。

**理想状态**：开工时按需开工具，用多少开多少，不堆。调研阶段开 search，复杂逻辑开 sequential-thinking，UI 改完开 Playwright 验收。

## 4. CI/部署后跟进

**现状**：写完代码 push 完就结束了，没有 CI 和部署验证环节。

**缺什么**：

- Hermes SOP 里的第 11 步：推送后看 CI 日志，有失败就修好再推
- 部署后检查线上——环境变量对不对、功能跑不跑得通、用户能不能看到
- 「把 localhost 当线上」是常见坑——dev server 跑通 ≠ 部署成功

**理想状态**：

```
push → 等 CI 跑完 → 看日志 → 有失败就修复 → 部署 → 线上验证 → 才算做完
```

## 5. Debug 流程

**现状**：Trellis 的 break-loop 是「修完 bug 后的根因分析和预防」，不是修 bug 本身。修复过程靠人工硬扛。

**缺什么**：

- Bug 复现步骤（能稳定重现才叫找到 bug）
- 定位链：设断点 → 看变量 → 追踪调用栈 → 定位根因
- Serena 的 JetBrains 后端能做交互式调试（设断点、逐步执行），但那是付费功能，LSP 免费后端做不到

**理想状态**：

```
复现 bug → 定位根因 → fix → 回归测试 → break-loop 五维分析（分类/根因/预防/扩散/固化）
```

## 缺口全景

```
                         ┌──────────┐
                    缺口2 │ 模型分工  │
                         └──────────┘
                              │
                              ▼
缺口3 ──→ 调研 ──→ brainstorm ──→ spec ──→ 开发 ──→ 测试 ──→ push ──→ CI/部署 ──→ 缺口4
 辅助工具                                      ▲                   │
                                               │                   │
                                          缺口1 测试            缺口5 Debug
```

核心链条（Trellis + GitNexus + Serena）管的是中间段；五个缺口分布在上游、下游和侧翼。

## 优先级

| 优先级 | 缺口 | 理由 |
| ------ | ---- | ---- |
| 🔴 高 | 测试环节 | 没有测试兜底，改完代码不知道有没有搞坏别的东西 |
| 🔴 高 | CI/部署跟进 | push 完不管，线上挂了都不知道 |
| 🟡 中 | 模型分工 | 影响产出质量，但不阻塞基本流程 |
| 🟡 中 | 辅助工具 | 有当然更好，没有也能干活 |
| 🟢 低 | Debug 流程 | 高级场景，大部分 bug 靠现有工具能定位 |

## 相关

- [[Trellis + GitNexus + Serena 工作流]] — 当前在用的核心工作流
- [[Trellis 工序与工具链]] — 社区工序 + 完整工具链（含辅助工具推荐）
- [[Code Change Workflow]] — 单次代码修改 SOP（含 TDD + CI 跟进）
- [[Serena]] — IDE 级语义代码操作（含 JetBrains 调试能力）
